@model IEnumerable<quiendamas.Models.Subasta>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>

@if (Request.IsAuthenticated)
{
    if (User.IsInRole("Administrador"))
    {
    @Html.ActionLink("Create New", "Create")
    }
}
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.articulo.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fechaInicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fechaFin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ganador)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.articulo.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fechaInicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fechaFin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ganador)
        </td>

@if (Request.IsAuthenticated)
{
    if (User.IsInRole("Administrador"))
    {
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.subastaID }) |
            @Html.ActionLink("Details", "Details", new { id = item.subastaID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.subastaID }) |
        </td>
    }
    else if (!User.IsInRole("Administrador"))
    {
        <td>
            @Html.ActionLink("Pujar", "Create", "Puja", new { id = item.subastaID }, null)
        </td>
    }

}
    </tr>
}

</table>
